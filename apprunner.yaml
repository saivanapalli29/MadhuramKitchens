version: 1.0
runtime: python311
build:
  commands:
    build:
      - python3 -m ensurepip --upgrade # Upgrade pip (optional)
      - pip3 install pipenv
      - pipenv install
      - pip3 install gunicorn==20.1.0
      - pip3 install Django # Install Django and dependencies
      - pip3 install -r requirements.txt
    post-build:
      - python3 manage.py makemigrations
      - python3 manage.py migrate
      - python3 manage.py collectstatic --noinput
  env:
    - name: DEBUG
      value: "on"
    - name: SECRET_KEY
      value: 'django-insecure-r(vctop4h_)2ciq#!38tr(bsr^qoyr%t$7*buweve1ikl=&1%0'
    - name: ALLOWED_HOSTS
      value: "*"
run:
  runtime-version: 3.11
  pre-run:  
  - pip3 install pipenv
  - pipenv install
  - python3 copy-global-files.py
  command: pipenv run gunicorn config.wsgi --log-file -
  network: 
    port: 8000
    env: 8000
  env:
    - name: DEBUG
      value: "on"
    - name: SECRET_KEY
      value: 'django-insecure-r(vctop4h_)2ciq#!38tr(bsr^qoyr%t$7*buweve1ikl=&1%0'
    - name: ALLOWED_HOSTS
      value: "*"
    - name: DATABASE_NAME
      value: "madhuramkitchen" 
    - name: DATABASE_HOST
      value: "madhuramkitchen.c3y62mus6911.ap-south-1.rds.amazonaws.com" 
    - name: DATABASE_USERNAME
      value: "admin" 
    - name: DATABASE_PASSWORD
      value: "saivanapalli"  
    - name: DATABASE_PORT
      value: "3306" 
